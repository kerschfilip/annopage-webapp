[project]
name = "annopage-webapp"
version = "0.1.0"
description = "Local web UI wrapper around annopage_client"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.23.0",
    "jinja2>=3.1.0",
    "lxml>=4.9.0",
    "requests>=2.31.0",
]
